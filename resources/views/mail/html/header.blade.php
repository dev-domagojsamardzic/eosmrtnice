@props(['url'])
<tr>
    <td class="header">
        <img style="height: 30px; width: auto;" src="{{ asset('images/logo/logo-light.svg') }}">
        <span>{{ config('app.name') }}</span>
    </td>
</tr>
